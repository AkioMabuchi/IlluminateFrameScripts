namespace Views.Instances.Frames
{
    public class FrameSmall : FrameBase
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
