using VContainer;
using VContainer.Unity;

namespace ForDevelopments
{
    public class InvestigationLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}